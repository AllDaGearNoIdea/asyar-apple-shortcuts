allowBuilds:
  esbuild: true
overrides:
  # This extension lives alongside the asyar repo, not inside it, so the
  # SDK is one level deeper than the in-repo `extensions/<name>` layout:
  #   ../asyar/asyar-sdk, not ../asyar-sdk.
  asyar-sdk: link:../asyar/asyar-sdk
